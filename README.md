# Three Fat Babies Website

Source for the Three Fat Babies website.

## Status

New build — starting fresh. An offline copy of the previous
Google Sites version is kept outside this repo in `../archive/`
for reference.

## Local preview

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.

## Deployment

Hosted with GitHub Pages from the `main` branch (root folder).
